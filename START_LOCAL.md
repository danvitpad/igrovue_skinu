# 🚀 Локальный запуск сайта

## Быстрый способ увидеть сайт

### 1. Откройте терминал в папке проекта

### 2. Установите зависимости для Frontend

```bash
cd frontend
npm install
```

### 3. Создайте файл .env.local

В папке `frontend/` создайте файл `.env.local` со следующим содержимым:

```
NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_STEAM_API_KEY=your_steam_api_key_here
NEXT_PUBLIC_STEAM_RETURN_URL=http://localhost:3000/auth/steam/callback
NEXT_PUBLIC_EPIC_CLIENT_ID=your_epic_client_id_here
```

### 4. Запустите Frontend

```bash
npm run dev
```

### 5. Откройте браузер

Перейдите на: **http://localhost:3000**

---

## Что вы увидите:

✅ **Главная страница** - Яркий дизайн с градиентами  
✅ **Страница логина** - /auth/login  
✅ **Страница регистрации** - /auth/register  
✅ **Маркетплейс** - /marketplace  

---

## Примечания:

- Backend API пока не подключен (UI будет работать, но функции авторизации/данные не будут работать)
- Для полной функциональности нужно также запустить backend и настроить БД
- Подробнее см. `DEVELOPMENT.md`
