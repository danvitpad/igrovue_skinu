# 🤖 Автоматизированный Deployment

**Максимальная автоматизация подключения сервисов с минимальным участием пользователя**

Версия: 1.0 | Для AI Assistants с MCP Browser | Январь 2025

---

## 🎯 Цель

Автоматизировать подключение всех сервисов:
- GitHub (репозиторий)
- Vercel (frontend)
- Render (backend + PostgreSQL)
- Stripe (платежи)
- Resend (email)
- Telegram (бот)
- Twilio (SMS, опционально)

**Время работы пользователя:** 5-10 минут (только авторизация и вставка ключей)
**Время работы AI:** 30-40 минут (все остальное)

---

## 📋 Процесс автоматизации

### Этап 1: Подготовка (AI автоматически)

1. **Проверка окружения**
   ```bash
   node --version
   npm --version
   git --version
   ```

2. **Инициализация Git (если нужно)**
   ```bash
   git init
   git config --global user.name "Your Name"
   git config --global user.email "your@email.com"
   ```

3. **Создание структуры конфигурации**
   - Создать `deployment/.secrets.json` (в .gitignore)
   - Создать шаблоны конфигов
   - Подготовить скрипты

### Этап 2: GitHub (AI + Пользователь)

**AI действия:**
1. Открывает https://github.com/login в браузере
2. Ждет авторизации пользователя
3. Открывает https://github.com/new для создания репозитория
4. Пользователь создает репозиторий и копирует URL

**Пользователь:**
- Авторизуется на GitHub
- Создает репозиторий (публичный или приватный)
- Копирует URL репозитория

**AI действия после:**
1. Получает URL от пользователя
2. Добавляет remote:
   ```bash
   git remote add origin <URL>
   ```
3. Делает первый коммит:
   ```bash
   git add .
   git commit -m "Initial commit: Skiny Hub"
   git push -u origin main
   ```

---

### Этап 3: Vercel (AI + Пользователь)

**AI действия:**
1. Открывает https://vercel.com/login в браузере
2. Ждет авторизации пользователя (можно через GitHub)
3. После авторизации открывает https://vercel.com/new

**Пользователь:**
- Авторизуется на Vercel (лучше через GitHub)
- Разрешает доступ к репозиторию

**AI действия после:**
1. Устанавливает Vercel CLI (если нужно):
   ```bash
   npm i -g vercel
   ```
2. Авторизуется через CLI:
   ```bash
   vercel login
   ```
3. Создает `vercel.json` с конфигурацией
4. Делает деплой:
   ```bash
   cd frontend
   vercel --prod
   ```
5. Получает URL от Vercel
6. Сохраняет в конфигурацию

**Переменные окружения для Vercel (AI настраивает через CLI):**
```bash
vercel env add NEXT_PUBLIC_API_URL production
# Вводит: https://PROJECT_NAME-backend.onrender.com (будет обновлено после Render)
```

---

### Этап 4: Render (AI + Пользователь)

**AI действия:**
1. Открывает https://dashboard.render.com/register в браузере
2. Ждет регистрации/авторизации пользователя
3. После авторизации открывает дашборд

**Пользователь:**
- Регистрируется/авторизуется на Render
- Авторизуется через GitHub (рекомендуется)

**AI действия после:**
1. Устанавливает Render CLI (если нужно):
   ```bash
   # Windows (PowerShell)
   iwr https://render.com/download/render-windows -OutFile render.exe
   # Или через scoop/chocolatey
   ```
2. Авторизуется:
   ```bash
   render login
   ```
3. Создает PostgreSQL базу данных:
   ```bash
   render databases create --name skinny-hub-db --database skinny_hub --user skinny_user --region oregon
   ```
4. Получает DATABASE_URL
5. Создает `backend/render.yaml`
6. Создает Web Service:
   ```bash
   render services create web --name skinny-hub-backend --repo <GITHUB_URL> --branch main --root-dir backend
   ```
7. Получает URL сервиса
8. Обновляет переменные окружения в Vercel

**Переменные окружения для Render (AI настраивает через CLI):**
```bash
render env set JWT_SECRET "<generated-secret>"
render env set RESEND_API_KEY "<from-user>"
render env set STRIPE_SECRET_KEY "<from-user>"
# И т.д.
```

---

### Этап 5: Stripe (AI + Пользователь)

**AI действия:**
1. Открывает https://dashboard.stripe.com/register в браузере
2. Ждет регистрации/авторизации
3. После авторизации открывает https://dashboard.stripe.com/test/apikeys

**Пользователь:**
- Регистрируется/авторизуется
- Переходит на страницу API ключей
- Копирует Publishable key (pk_test_...)
- Копирует Secret key (sk_test_...)

**AI действия после:**
1. Сохраняет ключи в конфигурацию
2. Добавляет в Vercel:
   ```bash
   vercel env add NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY production
   # Вводит ключ
   ```
3. Добавляет в Render:
   ```bash
   render env set STRIPE_SECRET_KEY "<secret-key>"
   ```
4. Создает Webhook в Stripe (через браузер или API):
   - URL: https://PROJECT_NAME-backend.onrender.com/api/webhooks/stripe
   - События: payment_intent.succeeded, charge.succeeded и т.д.
5. Получает Webhook Secret (whsec_...)
6. Сохраняет в Render:
   ```bash
   render env set STRIPE_WEBHOOK_SECRET "<webhook-secret>"
   ```

---

### Этап 6: Resend (AI + Пользователь)

**AI действия:**
1. Открывает https://resend.com/signup в браузере
2. Ждет регистрации
3. После регистрации открывает https://resend.com/api-keys

**Пользователь:**
- Регистрируется
- Создает API ключ (Create API Key)
- Копирует ключ (re_...)

**AI действия после:**
1. Сохраняет ключ
2. Добавляет в Render:
   ```bash
   render env set RESEND_API_KEY "<api-key>"
   ```
3. Добавляет FROM email:
   ```bash
   render env set EMAIL_FROM "noreply@yourdomain.com"
   ```

---

### Этап 7: Telegram (AI + Пользователь)

**AI действия:**
1. Открывает https://t.me/BotFather в браузере/Telegram
2. Инструкции для пользователя

**Пользователь:**
- Открывает @BotFather в Telegram
- Отправляет /newbot
- Следует инструкциям
- Копирует токен бота

**AI действия после:**
1. Сохраняет токен
2. Добавляет в Render:
   ```bash
   render env set TELEGRAM_BOT_TOKEN "<bot-token>"
   ```

---

### Этап 8: Финальная настройка (AI автоматически)

1. **Обновление URL'ов:**
   - Backend URL в Vercel env vars
   - Frontend URL в Render env vars

2. **Запуск миграций БД:**
   ```bash
   cd backend
   npm run db:generate
   npm run db:migrate
   # Или через Render dashboard
   ```

3. **Тестирование:**
   - Health checks
   - API endpoints
   - Frontend доступность

4. **Создание итогового файла с конфигурацией:**
   - Все URL'ы
   - Все ключи (хранятся локально, не коммитятся)

---

## 📝 Структура файлов

```
deployment/
├── .secrets.json          # Локальные секреты (в .gitignore)
├── config-template.json   # Шаблон конфигурации
└── auto-deploy.sh         # Скрипт автоматизации (опционально)
```

### .secrets.json (пример)

```json
{
  "github": {
    "repo_url": "https://github.com/username/skiny-hub.git"
  },
  "vercel": {
    "url": "https://skiny-hub.vercel.app",
    "project_id": "prj_xxx"
  },
  "render": {
    "backend_url": "https://skiny-hub-backend.onrender.com",
    "db_url": "postgresql://..."
  },
  "stripe": {
    "publishable_key": "pk_test_...",
    "secret_key": "sk_test_...",
    "webhook_secret": "whsec_..."
  },
  "resend": {
    "api_key": "re_..."
  },
  "telegram": {
    "bot_token": "123456:ABC..."
  }
}
```

---

## 🔄 Последовательность действий для AI

### Шаг 1: Инициализация
1. Проверить окружение
2. Создать deployment/.secrets.json
3. Добавить в .gitignore

### Шаг 2: GitHub
1. Открыть github.com/login
2. Ждать авторизации
3. Получить URL репозитория от пользователя
4. Настроить Git и сделать push

### Шаг 3: Vercel
1. Открыть vercel.com/login
2. Ждать авторизации
3. Использовать CLI для деплоя
4. Получить URL
5. Сохранить в конфиг

### Шаг 4: Render
1. Открыть dashboard.render.com
2. Ждать авторизации
3. Создать БД через CLI
4. Создать Web Service
5. Получить URL
6. Сохранить в конфиг

### Шаг 5: Stripe
1. Открыть dashboard.stripe.com/test/apikeys
2. Ждать авторизации
3. Получить ключи от пользователя
4. Настроить webhook
5. Сохранить все ключи

### Шаг 6: Resend
1. Открыть resend.com/api-keys
2. Ждать регистрации/авторизации
3. Получить API ключ
4. Сохранить

### Шаг 7: Telegram
1. Инструкции для пользователя
2. Получить токен
3. Сохранить

### Шаг 8: Финальная настройка
1. Обновить все URL'ы
2. Запустить миграции
3. Протестировать
4. Выдать итоговый отчет

---

## 🎯 Итоговый отчет

После завершения AI создает файл `DEPLOYMENT_SUMMARY.md`:

```markdown
# Deployment Summary

## URL'ы
- Frontend: https://skiny-hub.vercel.app
- Backend: https://skiny-hub-backend.onrender.com
- Database: PostgreSQL на Render

## Переменные окружения
Все настроены автоматически через CLI

## Следующие шаги
1. Настроить кастомный домен
2. Переключить Stripe на production
3. Настроить мониторинг
```

---

## ⚠️ Важные замечания

1. **Секреты никогда не коммитятся** - только локально в .secrets.json
2. **Все ключи собираются от пользователя** - AI только сохраняет и настраивает
3. **CLI используется везде где возможно** - максимальная автоматизация
4. **Браузер используется для авторизации** - пользователь просто логинится
5. **URL'ы обновляются автоматически** - после каждого деплоя

---

## 🚀 Быстрый старт для AI

```markdown
Привет! Я помогу автоматизировать deployment.

Шаг 1: Проверяю окружение...
Шаг 2: Открываю GitHub для авторизации...
Шаг 3: [После авторизации] Создайте репозиторий и дайте мне URL
Шаг 4: Открываю Vercel для авторизации...
...

[Пользователь только авторизуется и дает ключи]
[AI делает все остальное]
```
